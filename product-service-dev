server: 
  port: ${PORT:8087}

message: productService is live for dev

spring:
  application:
    name: product-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8760/eureka/
